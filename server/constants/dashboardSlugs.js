import { RETAIL_DASHBOARD_SLUG } from '../modules/retail/constants.js';

/** Dashboard link trees exposed from API / seed — Phase 1 slugs aligned with Postgres `dashboard_defs.slug`. */
export const PHASE_ONE_DASHBOARD_SLUGS = ['isd_nm', RETAIL_DASHBOARD_SLUG];
