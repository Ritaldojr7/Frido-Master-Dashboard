/**
 * Retail module (server) — public surface.
 * See src/modules/retail/README.md for the full module boundary and the
 * shared server dependencies that remain (notices table, dashboards route,
 * static-dashboard auth).
 */
export {
    RETAIL_DASHBOARD_SLUG,
    NOTICE_AUDIENCE_RETAIL,
    RETAIL_NOTICE_ROLES,
    RETAIL_STATIC_PREFIX,
} from './constants.js';
export { DEFAULT_RETAIL_STRUCTURE_CONTACTS } from './organization.js';
